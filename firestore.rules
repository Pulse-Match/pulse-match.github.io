rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Waitlist collection - allow anyone to add, but no read/update/delete
    match /waitlist/{document} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}
